# 统一换行符，避免 Windows 提交后 shell 脚本在 macOS/Linux 上因 CRLF 无法执行
* text=auto eol=lf

# 明确指定（双保险）
*.sh    text eol=lf
*.js    text eol=lf
*.json  text eol=lf
*.md    text eol=lf

# PowerShell 脚本在 Windows 上用 CRLF 更稳
*.ps1   text eol=crlf

# 二进制
*.png   binary
*.jpg   binary
*.pdf   binary
